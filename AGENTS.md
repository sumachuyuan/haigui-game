# AI海龟汤游戏开发指令

## 项目概述
面向幼儿园到小学低年级孩子的AI海龟汤游戏，使用 React + TypeScript + Tailwind CSS 开发。支持语音输入、孩子编号、互动录像等功能。

## 开发规范
- 使用 TypeScript，确保类型安全
- 使用函数式组件 + Hooks
- 使用 Tailwind CSS 编写样式
- 组件要可复用
- **所有文本内容需经过儿童友好审核**（避免成人化、恐怖表达）
- **语音输入优先**：孩子用语音提问，老师可复核修改后发送
- **录像功能**：有明确的开始和停止机制（孩子确认编号后开始，换人/结束时停止，每人一段独立视频）

## 代码风格
- 组件名使用 PascalCase（如 `StartScreen`、`ChatBox`）
- 函数名使用 camelCase（如 `handleSend`、`startRecording`）
- 常量使用 UPPER_SNAKE_CASE（如 `MAX_QUESTIONS`）
- 类型定义以 T 开头（如 `TStory`、`TMessage`）
- **所有组件必须有注释说明其作用**（方便我理解和后续调整）

## 设计要求
- **品牌形象**：以机构 Logo 为主角，不使用海龟
- **整体风格**：科技感 + 童趣的融合
- **主色调**：蓝色调（体现科技感，与市面上橙黄主色调的机构拉开差距）
- **背景**：纸张纹理 + 蜡笔涂鸦元素
- **字体**：手写感字体（像孩子用蜡笔写的）
- **按钮**：椭圆形、大而圆润、半透明白色气泡感
- **整体感觉**：像孩子用蜡笔画出来的，但又带科技感的混合体

## 注意事项
- 保持代码简洁，避免过度设计
- 优先实现核心功能
- 确保移动端适配
- **API Key 不要硬编码，使用环境变量**（创建 `.env.local` 文件存放）
- **孩子编号输入框需支持数字键盘**（移动端体验）
- **所有交互都要有明确的加载状态**
- **加载状态要有趣**：可用品牌 Logo 卡通形象做动画（如“吃东西”、“转圈圈”等）
- **AI 回答超时时**：显示友好提示，老师可直接控场

## 测试要求
- 每个功能完成后手动测试
- 确保各种屏幕尺寸正常显示
- 测试 AI 回答是否准确
- **测试孩子用语音输入时，识别是否准确**
- **测试编号输入后，录像能否正确对应到每个孩子**
- **测试 AI 对模糊问题的温柔引导是否有效**（不是冷冰冰的“无关”）